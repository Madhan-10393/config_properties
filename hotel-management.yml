# we will run VM arguments, because we are applying multiple instances and load balancing
#server:
#  port: 1111

spring:
#  application:
#    name: hotel-management
  boot:
    admin:
      client:
        url: http://localhost:8044/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/

management:
  endpoints:
    web:
      exposure:
        include: '*'